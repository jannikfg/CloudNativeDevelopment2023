version: '3.9'

services:
  postgres-userservice:
    image: postgres:15.0
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=analyticsservice